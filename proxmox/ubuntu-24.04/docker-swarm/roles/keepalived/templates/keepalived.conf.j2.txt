vrrp_script chk_docker {
    script "pidof dockerd"
    interval 2
    weight 2
}

vrrp_instance VI_1 {
    state BACKUP
    interface eth0
    virtual_router_id {{ virtual_router_id }}
    priority {{ 110 if inventory_hostname == groups['docker_managers'][0] else 100 }}
    advert_int 1
    authentication {
        auth_type PASS
        auth_pass {{ auth_pass }}
    }
    virtual_ipaddress {
        {{ vip_address }}
    }
    track_script {
        chk_docker
    }
}