# Example secrets file - Copy this to secrets.auto.tfvars and update with your values
# DO NOT commit secrets.auto.tfvars to version control!

# Proxmox API Token ID (format: username@realm!token-name)
proxmox_api_token_id="root@pam!your-token-name"

# Proxmox API Token Secret (UUID format)
proxmox_api_token_secret="xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx"

# VM User credentials
vm_user="your-vm-username"
vm_password="your-secure-password"